services:
  - type: web
    name: leadhunter-research-worker
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: LOG_LEVEL
        value: INFO
      - key: SEARXNG_URL
        value: https://searx.tiekoetter.com
      - key: SEARXNG_TIMEOUT
        value: "20"
